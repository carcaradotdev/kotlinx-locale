# The Kotlin/JS size probes, one directory under tools/ per line.
#
# Read by settings.gradle.kts to include the projects, and by the
# kotlinx-locale-verification convention plugin to depend on their reports.
# One list, so a probe cannot be built without also being reported on.
probe-locale
probe-country-codes
probe-country-full
probe-country-platform
probe-currency-codes
probe-currency-full
probe-currency-plurals
probe-currency-platform
probe-datetime-full
probe-datetime-skeletons
probe-datetime-platform
probe-datetime-relative
probe-datetime-durations
probe-language-full
probe-number-full
probe-timezone-full
probe-timezone-cities
probe-phone-full
probe-everything
probe-everything-platform
probe-datetime-intervals
probe-personname-full
probe-collation-full
