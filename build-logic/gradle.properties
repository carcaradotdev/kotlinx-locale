# Mirrors the root build's baseline so that running this build on its own
# (./gradlew -p build-logic validatePlugins) behaves the same as running it as an
# included build.
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true

kotlin.code.style=official
