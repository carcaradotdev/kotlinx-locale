// GENERATED by :codegen from CLDR release-48-2. Do not edit.
// Regenerate with: ./gradlew :codegen:generateLocaleData
package dev.carcara.kotlinx.locale.language.cldr

import dev.carcara.kotlinx.locale.Locale
import dev.carcara.kotlinx.locale.language.LanguageDisplay
import dev.carcara.kotlinx.locale.language.LanguageNameSource
import dev.carcara.kotlinx.locale.language.LanguageNameStyle
import dev.carcara.kotlinx.locale.language.cldr.internal.data.localeDisplayNamesRegistry
import dev.carcara.kotlinx.locale.language.cldr.runtime.PayloadLanguageNames
import dev.carcara.kotlinx.locale.language.displayName
import dev.carcara.kotlinx.locale.language.languageName
import dev.carcara.kotlinx.locale.language.nativeDisplayName
import dev.carcara.kotlinx.locale.language.regionName
import dev.carcara.kotlinx.locale.language.scriptName

/**
 * The language, script and region names this build carries.
 *
 * The lookup and the display name algorithm live in
 * `kotlinx-locale-language-cldr-runtime` and `-core`; all this object
 * contributes is the table.
 */
public object CldrLanguage : LanguageNameSource by PayloadLanguageNames(localeDisplayNamesRegistry)

/**
 * This locale's name written in [inLocale], e.g. `Brazilian Portuguese` for
 * `pt-BR` in `en`.
 *
 * [display] chooses between CLDR's own dialect name and one composed from the
 * language plus its subtags: `British English` against
 * `English (United Kingdom)`.
 */
public fun Locale.displayName(
    inLocale: Locale = Locale.current,
    display: LanguageDisplay = LanguageDisplay.DIALECT,
    style: LanguageNameStyle = LanguageNameStyle.STANDARD,
): String = CldrLanguage.displayName(this, inLocale, display, style)

/**
 * This locale's name in its own language: `日本語`, `čeština`, `português`.
 *
 * CLDR stores these as the language writes them, which is lower case in
 * several. A picker row that wants `Čeština` is asking for a capitalization
 * transform, which is a different question from what the name is.
 */
public val Locale.nativeDisplayName: String get() = CldrLanguage.nativeDisplayName(this)

/** The name of the language [subtag] in this locale; falls back to the subtag. */
public fun Locale.languageName(subtag: String, style: LanguageNameStyle = LanguageNameStyle.STANDARD): String =
    CldrLanguage.languageName(subtag, this, style)

/** The name of the ISO 15924 script [code] in this locale, e.g. `Latin` for `Latn`. */
public fun Locale.scriptName(code: String): String = CldrLanguage.scriptName(code, this)

/**
 * The name of the region [code] in this locale.
 *
 * Wider than the country domain's: this answers for macro-regions too, so
 * `419` reads `Latin America`.
 */
public fun Locale.regionName(code: String): String = CldrLanguage.regionName(code, this)
